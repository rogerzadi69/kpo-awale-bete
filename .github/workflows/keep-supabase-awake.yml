name: Keep Supabase Awake

on:
  schedule:
    - cron: "17 6 */3 * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase public API
        env:
          SUPABASE_URL: https://biewgotuyisdjvqloaqw.supabase.co
          SUPABASE_PUBLISHABLE_KEY: sb_publishable_2451OGLMsUJz0XBvCcpPeg_yH11sKYW
        run: |
          set -euo pipefail
          curl --fail --silent --show-error \
            --header "apikey: ${SUPABASE_PUBLISHABLE_KEY}" \
            "${SUPABASE_URL}/auth/v1/settings" > /dev/null
